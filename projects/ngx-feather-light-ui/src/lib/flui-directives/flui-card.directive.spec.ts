import { FluiCardDirective } from './flui-card.directive';

describe('FluiCardDirective', () => {
  it('should create an instance', () => {
    const directive = new FluiCardDirective();
    expect(directive).toBeTruthy();
  });
});
