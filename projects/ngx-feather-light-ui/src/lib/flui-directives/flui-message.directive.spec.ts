import { FluiMessageDirective } from './flui-message.directive';

describe('FluiMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new FluiMessageDirective();
    expect(directive).toBeTruthy();
  });
});
